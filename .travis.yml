language: node_js
node_js:
  - "6"
  - "8"
dist: trusty
sudo: false

env:
  global:
    - LOGS_DIR=reports

branches:
  only:
  - master

cache:
  directories:
    - node_modules
    - packages/stark-build/node_modules
    - packages/stark-core/node_modules

addons:
  chrome: stable

script:
  - npm run lint:starter
